[pytest]
# Suite autonome : l'API tourne dans le processus de test (base SQLite
# temporaire, services externes simulés). Aucun serveur ni réseau requis.
#   cd backend && pytest
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
